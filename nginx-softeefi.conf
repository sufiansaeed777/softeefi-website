server {
    listen 80;
    server_name softeefi.co.uk www.softeefi.co.uk;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    # Frontend - React build files
    # Path must match the rsync target in .github/workflows/deploy.yml
    location / {
        root /var/www/softeefi-git/frontend/build;
        try_files $uri $uri/ /index.html;

        # HTML: very short TTL so deploys propagate quickly
        location = /index.html {
            expires 5m;
            add_header Cache-Control "public, must-revalidate" always;
        }

        # Hashed JS/CSS bundles: cache forever (file names change on every build)
        location ~* \.(js|css)$ {
            expires 1y;
            add_header Cache-Control "public, immutable" always;
        }

        # Images, fonts, icons: cache 1 year (paths are stable)
        location ~* \.(png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable" always;
        }
    }

    # Backend API proxy
    location /api {
        proxy_pass http://localhost:4001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeout settings
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # File upload size
    client_max_body_size 10M;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/javascript application/xml+rss application/json;
    gzip_min_length 1000;
    gzip_comp_level 6;
    gzip_vary on;
    gzip_disable "msie6";

    # Logs
    access_log /var/log/nginx/softeefi.access.log;
    error_log /var/log/nginx/softeefi.error.log;
}

# HTTPS configuration (will be added by certbot)
# server {
#     listen 443 ssl http2;
#     server_name softeefi.co.uk www.softeefi.co.uk;
#     
#     ssl_certificate /etc/letsencrypt/live/softeefi.co.uk/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/softeefi.co.uk/privkey.pem;
#     
#     # ... rest of configuration same as above ...
# }